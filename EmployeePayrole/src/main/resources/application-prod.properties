# Database Connection (Production)
spring.datasource.url=jdbc:mysql://localhost:3306/employeedb
spring.datasource.username=employeeuser
spring.datasource.password=employee@123

# Hibernate Settings (Production)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=validate  # Prevents automatic schema updates
spring.jpa.show-sql=false  # Don't log SQL queries in production
